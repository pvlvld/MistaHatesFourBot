import type { ChatSettings, MyContext } from '../types/grammy.types';

export function getChatSettings(ctx: MyContext, settings: ChatSettings) {}

export function setChatSettings(ctx: MyContext, settings: ChatSettings) {}
